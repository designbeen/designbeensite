import colors from '../colors.js';
import typography from '../typography.js';
import spacing from '../spacing.js';
import effects from '../effects.js';

const designbeenTheme = {
  name: 'designbeen',
  brand: {
    name: 'DesignBeen',
    slogan: 'Engineering Future Realities',
  },
  colors,
  typography,
  spacing,
  effects,
};

export default designbeenTheme;
