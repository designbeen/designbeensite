# MySQL Database Credentials
DB_HOST=localhost
DB_PORT=3306
DB_NAME=designbeen
DB_USER=root
DB_PASSWORD=
DB_SSL=false

# Alternatively, single MySQL connection string (PlanetScale, Railway, Aiven, etc.)
# DATABASE_URL=mysql://user:password@host:3306/database?sslaccept=strict

# Authentication Secret
JWT_SECRET=change-me-in-production-jwt-secret-key-12345

# Max Upload Size in Bytes (default 10MB)
MAX_IMAGE_UPLOAD_BYTES=10485760
